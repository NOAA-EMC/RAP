#BSUB -J %E%rap_forecast_pcyc_%CYC%
#BSUB -o  /com/output/%ENVIR%/today/rap_forecast_pcyc_%CYC%.o%J
#BSUB -e  /com/output/%ENVIR%/today/rap_forecast_pcyc_%CYC%.o%J
#BSUB -cwd /tmpnwprd
#BSUB -q prod
#BSUB -L /bin/sh
#BSUB -P %PROJ%
#BSUB -n 1200 
#BSUB -R "span[ptile=24]"
#BSUB -W 00:40
#BSUB -x
#BSUB -a poe

%include <head.h>

# EXPORT list here

set -x

export envir=%ENVIR%
export cyc=%CYC%
export job=rap_forecast_pcyc_%CYC%

# versions file for Rapid Refresh model $mod_ver
VERSION_FILE=/nw${envir}/versions/rap.ver
if [ -f $VERSION_FILE ]; then
  . $VERSION_FILE
else
  ecflow_client --abort
  exit
fi

# CALL executable job script here
/nw${envir}/rap.${model_ver}/jobs/JRAP_FORECAST_PCYC

if [ $? -ne 0 ]; then
  ecflow_client --abort
  exit
fi

%include <tail.h>
%manual
##########################################################################
# Purpose: to prepare the boundary conditions for the Rapid Refresh Model
##########################################################################


######################################################################
# Job specific troubleshooting instructions:
#  see generic troubleshoot manual page
#
######################################################################

# include manual page below
%end
