#BSUB -J %E%rap_analysis_%CYC%
#BSUB -o  /%COM%/output/%ENVIR%/today/rap_prep_smoke_pcyc_%CYC%.o%J
#BSUB -e  /%COM%/output/%ENVIR%/today/rap_prep_smoke_pcyc_%CYC%.o%J
#BSUB -cwd /tmpnwprd
#BSUB -R "span[ptile=24]"
#BSUB -R affinity[core:distribute=balance]
#BSUB -n 24
#BSUB -q %QUEUE%
#BSUB -L /bin/sh
#BSUB -W 00:25
#BSUB -P %PROJ%
#BSUB -x
#BSUB -a poe

%include <head.h>
%include <envir-p2.h>

# EXPORT list here

set -x

export cyc=%CYC%

# versions file for Rapid Refresh model $mod_ver
VERSION_FILE=$NWROOT/versions/rap.ver
if [ -f $VERSION_FILE ]; then
  . $VERSION_FILE
else
  ecflow_client --abort
  exit
fi

# CALL executable job script here
$NWROOT/rap.${rap_ver}/jobs/JRAP_PREP_SMOKE_PCYC

if [ $? -ne 0 ]; then
  ecflow_client --abort
  exit
fi

%include <tail.h>
%manual
##########################################################################
# Purpose: to prepare the boundary conditions for the Rapid Refresh Model
##########################################################################


######################################################################
# Job specific troubleshooting instructions:
#  see generic troubleshoot manual page
#
######################################################################

# include manual page below
%end
